name: user_repository
description: A repository to manage the user domain.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  api_client:
    path: ../api_client
  api_models:
    path: ../api_models

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.7
  very_good_analysis: ^10.0.0-rc.2
